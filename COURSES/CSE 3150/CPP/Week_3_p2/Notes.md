`#ifndef` means "If Not Defined" and is used to guard against accidentally defining or including something multiple times in a header file.

Alternatively, you can use `#pragma once` to say that you want to define the contents of this file *only* if it is not already defined.
### std::cin