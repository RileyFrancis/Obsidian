You can create a new tmux session using the command `tmux`. 

- Exit and save the tmux session: `Ctrl+B, D`.  
- Reattach an existing tmux session: `tmux attach`
- Split vertically: `Ctrl+B, "`
- Split horizontally: `Ctrl+B, %`
- Switch between split windows: `Ctrl+B, arrow-keys`
